# Deep‑learning framework
torch>=2.0.0                # PyTorch (provides the tensor core)

# Audio / video handling
torchaudio                  # matches the installed torch version
moviepy                     # video I/O and audio extraction
ffmpeg-python               # thin wrapper used by moviepy (ffmpeg binary must be installed on the OS)

# Model & tokeniser
git+https://github.com/huggingface/transformers.git  # latest 🤗 Transformers (required for SeamlessM4T)
sentencepiece  # SentencePiece tokenizer used by the model

# TTS Engines (install as needed)
pipmaster
# TTS>=0.22.0  # Coqui XTTS v2 (recommended)
# git+https://github.com/suno-ai/bark.git  # Bark (uncomment if needed)
# git+https://github.com/yl4579/StyleTTS2.git  # StyleTTS2 (uncomment if needed)
# piper-tts  # Piper (uncomment if needed)

fastapi
uvicorn
python-multipart
websockets
yt_dlp
tiktoken
sentencepiece
protobuf<4
soundfile
librosa
python-dotenv
git+https://github.com/SWivid/F5-TTS.git
demucs
vocos

lollms-client
ascii-colors